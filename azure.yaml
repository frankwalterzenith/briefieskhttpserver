# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: KernelHttpServer
metadata:
    template: azd-init@1.4.2
services:
    KernelHttpServer:
        project: .
        host: containerapp
        language: dotnet
